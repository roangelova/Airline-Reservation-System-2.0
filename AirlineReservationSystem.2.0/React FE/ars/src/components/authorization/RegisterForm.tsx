import React from 'react';

const RegisterForm: React.FC = () => {

    return (
        <section className="register">
        <p> This is the register page. We are currently testing the router </p>
        </section>
    )
}

export default RegisterForm;